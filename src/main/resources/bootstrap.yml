spring:
  application:
    name: hello-service
  main:
    allow-bean-definition-overriding: true
  cloud:
    kubernetes:
      enabled: true
debug: true
---
spring:
  profiles: local
  cloud:
    kubernetes:
      enabled: false
myprops:
  name: haifeng

